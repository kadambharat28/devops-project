apiVersion: v1
kind: Service

metadata:
  name: devops-project-service

spec:
  type: NodePort

  selector:
    app: devops-project

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
